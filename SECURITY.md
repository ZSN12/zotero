# 安全策略（Security Policy）

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| 0.4.x | ✅ 当前支持 |
| < 0.4 | ❌ 不支持（见 CHANGELOG） |

## 报告漏洞

**不要在公开 issue 里报告安全漏洞。** 请通过以下渠道私下披露：

- GitHub Security Advisories（仓库公开后的首选通道：Security →
  Report a vulnerability）；
- 在仓库公开前：直接联系维护者（仓库 README/NOTICE 中的 contributors）。

请尽量提供：复现步骤/最小样例（如恶意构造的 DXF）、影响面评估、
你建议的缓解方案。我们会在 72 小时内确认收到，修复后致谢报告者
（除非要求匿名）。

## 项目安全边界（阅读代码前先看）

- **离线管线**：主管线（DXF → 杆件模型）不发起任何网络请求；
  MLLM 后端（kimi/openai）是可选 OCR 通道，未配置密钥时走
  NullBackend 优雅降级。CI 不调用任何外部 API。
- **不受信输入**：管线处理用户提供的 DXF/BOM 文件（ezdxf 解析）。
  在不受信环境下批量处理外来图纸时，请以独立进程/容器隔离运行
  （解析器崩溃即崩，不越权）。
- **密钥纪律**：任何 API key 不得进仓库。评测产物的
  `version.json` 带 `gt_injected` 披露段（z-only 层表注入显式声明），
  这是诚实性要求，也是审计通道——篡改该段会破坏证据链复现。
- **依赖披露**：第三方许可证归因见 `NOTICE`。
