# 35A1-JC1 未实现计划与事实基准（权威行动清单）

> **文档定位**：这是「结论已定、但尚未实现」的**唯一权威行动清单**。
> 防止上下文切换后把「已探查清楚但没执行」和「已做完」搞混。
> 任何后续开发/评测/agent 接手前，**先读本文件**。
>
> 日期：2026-08-31　状态：持续更新

---

## 0. 一分钟速览（TL;DR）

- **A2 召回 1.9% 的根本原因已定位，不是玄学**：评测口径不对称 + 横隔被误标 derived。
- **最大的可执行杠杆**：横隔（295 根 = 28% GT 杆）模型已生成但被排除评测，改判物理杆后
  召回 1.9%→4.8%（已实测）。
- **底段 z[0,5500] 无 DXF 源**，是硬缺口（不能用 GT 补，红线）。
- **40 段已从 merge 移除**（未提交，见 §3.1）。
- **三个待裁决决策**（见 §4）阻塞后续推进，需用户拍板。

---

## 1. 已定论的核心事实（不可再推翻，除非新证据）

### 1.1 塔身几何真值

| 项目 | 值 | 状态 |
|---|---|---|
| GT 全塔高 | z ∈ [0, 36600] mm | 已核实 |
| GT 底脚半宽 | ±2762 mm | 已核实 |
| GT 横担悬挑半宽 | ±2200 mm | 已核实 |
| GT 收分曲线 | half(z) = 2762 − 0.07·z（线性） | 已核实 |
| 模型全塔高 | z ∈ [0, 36606] mm（ΔZ=6.4mm，733.8mm 漂移已消除） | ✅ 已完成 |

### 1.2 各段半宽对齐（overlay 波段同带对比）

| 段 | z 范围 | GT 半宽 | 模型半宽 | 状态 |
|---|---|---|---|---|
| 40 | 0–5500 | ±2762→±2412 | —（详图，已移除） | ❌ 底段无源 |
| 07 | 5500–11000 | ±2342 | ±2332 | ✅ 对齐 |
| 06 | 11000–16000 | ±1992 | ±1926 | ✅ 对齐 |
| 05 | 16000–23000 | ±1642 | ±1576 | ✅ 对齐 |
| 04 | 23000–29800 | ±1082 | ±1077 | ✅ 对齐 |
| 02 | 30000–36600 | ±2200（横担） | ±1352 | ⚠️ 横担图纸不完整 |

### 1.3 A2 召回 1.9% 的三个精确根因（GT 1071 根杆构成）

| 根因 | GT 杆数 | 占比 | 可修复性 |
|---|---|---|---|
| ① 横隔被标 derived 排除 | 295 | 28% | **可修（口径/语义）** |
| ② 斜材节间细分差异 | 520 | 49% | 部分可修（收益有限） |
| ③ 主腿多变体叠加 | 252 | 24% | 不可修（GT 自矛盾） |

### 1.4 评测口径不对称（根因 ① 的本质）

- `eval_a2_geometry_2d` 用 `mode="recognition"`，模型侧只统计
  `geometry_class=="recognized"`（513 根，全 front 面）。
- GT 侧 `gt_bars_2d` 是**全部 1071 根物理杆**（含横隔 295 + 斜材 520 + 主腿 252）。
- **两侧口径不对称**，是召回 1.9% 的第一性原因。

### 1.5 模型杆件全景（2548 根）

| 分类 | 数量 | A2 是否统计 |
|---|---|---|
| recognized（front 直接识别） | 513 | ✅（recognition 模式） |
| reconstructed（b/l/r 镜像面） | 1539 | ❌ |
| derived（横隔 374 + 角腿 + 中心轴） | 496 | ❌ |

---

## 2. 关键代码位置（改错会翻车，先定位）

| 事项 | 文件 | 行/函数 |
|---|---|---|
| 横隔生成（22 杆十字拓扑） | `traceability/solve/tower_geometry.py` | `generate_diaphragms` (L895) |
| 横隔打标 `face="diaphragm"` | 同上 | L1016-1018 |
| derived 判定（含 diaphragm） | `traceability/eval/metrics.py` | `is_derived_bar` (L45) |
| physical 判定 | 同上 | `is_physical_bar` (L146) |
| A2 评测（recognition 模式） | 同上 | `eval_a2_geometry_2d` (L632) |
| 共线合并（C1 已修） | `traceability/intake/tower_dxf.py` | `_merge_collinear_fragments` (L292) |
| merge 集合消费 | `traceability/intake/tower_spec.py` | `sheet_is_spatial_mergeable` / L581 |

---

## 3. 已执行但未提交的改动（⚠️ 需要 commit）

### 3.1 layer_overlay.json（未提交）

- ✅ `merge_stems_extra` 移除 `35A1-JC1-40`（现为 `['07','06','05','04']`）
- ✅ `sheet_catalog['35A1-JC1-40']` 改为 `node_detail`
- ⚠️ `view_regions['35A1-JC1-40'].kind` 仍是 `front`（未改，因 40 已从 extra 移除，不影响）
- ⚠️ `bar_layers_by_stem['35A1-JC1-40']` 仍是 `['1','4','7']`（未改，同上不影响）

**注意**：`sheet_catalog` 全仓**无代码消费方**（仅文档提及），改它纯语义对齐，
**不能指望它拦住 40**。真正的开关是 `merge_stems_extra`（已移除）。

### 3.2 docs/JC1_SINGLE_TOWER_PLAN.md（未提交）

- 已被精简为「权威定稿版」（用户本人 2026-08-30 19:12 重写）。
- 我追加了 §3.0「A2 召回 1.9% 的精确根因」和 §5「待裁决决策点」。

### 3.3 PHASE_PROGRESS.md（未提交）

- 用户本人改的（认知订正注记）。

---

## 4. 待裁决决策点（⚠️ 阻塞后续，需用户拍板）

### D1：A2 评测口径（横隔/镜像面是否纳入）

- **D1-a（推荐）**：A2 改 `mode="physical"`，纳入 reconstructed（镜像面 + 横隔）。
- **D1-b**：保持 recognition，但 GT 侧同步排除横隔（1071→776）。
- **D1-c**：横隔只进 M3，A2 保持 recognition。

### D2：横隔语义归属（physical vs derived）

- **D2-a（推荐）**：横隔改判 physical（`geometry_class="reconstructed"`，保留 `diaphragm=True`
  作溯源但不再触发 derived 排除）。
- **D2-b**：保持 derived，横隔永久不进评测。

### D3：GT 注入约束最终口径

- **D3-a**：允许「GT 拓扑注入，几何从 DXF 解算」。
- **D3-b**：连 GT 坐标也注入（全量重建，A2 可 ~100%，此前用户已否决）。
- **D3-c**：维持不注入，A2 只做相对改进。

> **主线程建议**：D1-a + D2-a + D3-a。
> 理由：横隔是模型**已生成**的真实杆件，只因标记语义被排除；改判是「纠正口径」而非「注入 GT」。

### D4：底段缺口处理

**状态：10-14 已定性（见 §5.4），底段仍无源，结论确定。**

1. 接受缺口（移除 40 后 z[0,5500] 无源）。
2. 另寻图册（本册确无底段立面——全册穷尽：最大底宽 4594mm=07 号，距 z=0 所需 5524mm 差 21.9%）。
3. ~~复核 10–14~~ → **已定论**：10-14 是「各呼高的塔身上段」，最低 14 起于 z=7,500，不覆盖 z=0。

---

## 5. 已实测量化结果（可复现）

### 5.1 横隔改 physical 后的 A2 召回

实验：374 根横隔杆 `diaphragm→False, face→'f', geometry_class→reconstructed`，重跑 A2。

| tol | 召回前 | 召回后 |
|---|---|---|
| 50mm | 0.0% | 0.0% |
| 100mm | 0.0% | 0.1% |
| 200mm | 0.1% | 0.7% |
| 500mm | 1.9% | **4.8%** |

**为何不是 +28%**：模型横隔 z 生成在 DXF 腿节点处，腿节点 z 与 GT 节间 z 偏
**500-800mm**（GT 11500→模型 12090 Δ=590mm）。这是「阶段 5.1 锚点 Z 映射」的
**节间层残留**——全塔顶底已对齐，但内部节间 z 未对齐。

### 5.2 横隔节点可从腿节点精确推导

从 DXF 腿节点（4 角，12mm 精度）推导面心 `(±half,0),(0,±half)` + 内部
`(±half/2,±half/2)`，与 GT 横隔节点误差 **0-1mm**（z=11500/14000/16000 三水平全验证）。

### 5.3 模型横隔 z vs GT 横隔 z 对齐

GT 横隔 18 个水平，模型 33 个（过度生成），且 z 偏 500-800mm。
**根因**：横隔生成在 DXF 腿节点 z 处，腿节点 z 本身偏 GT 节间。

### 5.4 10-14 号图定性（2026-08-31 并行探查，收分斜率判决）

**结论：10-14 是塔身分段立面图（非横担模块），但都不覆盖底段 z=0。**

判决性证据是**收分斜率**（尺度无关）：塔身线性收分 half(z)=2762−0.07z ⟹
各向同性比例下必有 `(底宽−顶宽) = 0.14 × 段高`。

| sheet | 实测收分 | 理论 0.14×高 | 比值 |
|---|---|---|---|
| 07/06/05/04（对照） | 42.1/38.6/52.6/51.2 | 42.2/38.7/52.8/51.4 | **全 1.00** |
| 14/13/12/11/10 | 63.1/42.1/26.5/59.6/38.6 | 63.3/42.2/26.6/59.8/38.7 | **全 1.00** |

误差 <0.4%。旁证：主腿倾角实测 86.0° = atan(0.07)=4.005° 偏离竖直；
图上横向标注与实测腿间距×20 精确吻合（14: 4314=4314，10: 2844=2844）。

**z 区间（+80mm 半宽修正后，对 overlay 既有指派复原误差 0）**：

| sheet | 图上底宽 | 段高 | z 区间 |
|---|---|---|---|
| 14 | 4314 | 8500 | 7,500 → 16,000 |
| 13 | 3894 | 5500 | 10,500 → 16,000 |
| 12 | 3544 | 3000 | 13,000 → 16,000 |
| 11 | 3264 | 8000 | 15,000 → 23,000 |
| 10 | 2844 | 5000 | 18,000 → 23,000 |

**底段结论：否**。最低的 14 起于 z=7,500（底宽 4314mm），底段 z=0 需 5524mm，
短 21.9%（方法残差仅 ±1.4%）。全册穷尽最大底宽 4594mm（07 号），15-46 最大仅 2292mm。

**⚠️ 运维警告**：不要把 10-14 加进 `merge_stems_extra`——它们与 05/06/07 在
z 7,500-23,000 大面积重叠（14⊇06 全部、11⊇05 大部分、10⊂05），合并会产生重复杆件。
若要召回，必须先按 z 区间 + 半宽指纹去重。

**确定性**：10-14 是塔身立面（确定）；z 区间（确定）；底段无源（确定）；
「各呼高上段」解释（存疑，缺下段图纸佐证）。

---

## 6. 未实现待办清单（按优先级）

### P0：提交已完成的改动 ✅ 已完成（commit 4c5de56 / 645c13d）

- [x] `layer_overlay.json`（移除 40）+ 文档改动已提交。

### P1：横隔改判 physical（D1-a + D2-a）✅ 代码已改（待提交）

- [x] `tower_symmetry.py`：横隔 `evidence_status="reconstructed"` + `geometry_class="reconstructed"`
      （`is_diaphragm` 从 derived 分支拆出；corner_leg/center 仍 derived）。
- [x] `metrics.py` `is_derived_bar`：diaphragm 不再判 derived（保留 corner_leg/auto_diaphragm/center）。
- [x] `metrics.py` `eval_a2_geometry_2d`：mode recognition → physical（D1-a 口径对称）。
- [x] `metrics.py` view 过滤：横隔（face=diaphragm）不按 face 过滤，进任意 view 投影。
- [x] 测试更新（test_eval_metrics.py / test_run_manifest.py）+ 全量 315 passed。
- **实测 A2 结果**：模型杆 489→819（横隔 +330 进 physical），但召回仅 1.9%→**2.2%**
  （20→24 匹配），**远低于预期**。根因见 P2 新发现。

### P2：段 z_offset 系统偏差 -1143mm（精确根因已定位）← 当前最高优先级

- 横隔改判 physical 后，模型横隔 front 投影与 GT 横隔**端点中位差 1192mm**（min 495mm），
  一根都匹配不上（<200mm 为 0/295）。
- **精确根因（2026-08-31 实测，铁证）**：overlay 各段 `z_offset` 是「段高累加」得到的
  （5500/11000/16000/23000/30000），而真实塔的段底标高是「半宽反查」的。
  两者差一个**精确常数 +1143mm**：

  | 段 | 模型底半宽 | 真实z(半宽反查) | overlay z_offset | 偏差 |
  |---|---|---|---|---|
  | 07 | 2297mm | 6643 | 5500 | **+1143** |
  | 06 | 1912mm | 12143 | 11000 | **+1143** |
  | 05 | 1562mm | 17143 | 16000 | **+1143** |
  | 04 | 1077mm | 24071 | 23000 | +1071 |

  **根因**：底段 z[0,6643] 图纸缺失，段高累加从 5500 而非 6643 开始，导致全塔
  所有段整体偏低 1143mm。这是「阶段 5.1」只校正了顶底漂移（733.8mm）、
  未校正段间 z_offset 系统偏差的残留。

- [ ] **修复方案**：overlay 各段 z_offset 各 +1143mm（07→6643, 06→12143, 05→17143,
      04→24143, 02→31143）。z_span_mm 不变。
- [ ] 修复后横隔 z 将对齐 GT 横隔平台（6500/8500/11500/...），A2 召回预期显著提升。
- [ ] ⚠️ 风险：改全局 z_offset 影响全塔坐标，需重跑管线 + 全量回归 + A2/M3 验证。

**本轮实测结果（2026-08-31）**：
- 已实施 z_offset +1143（07→6643/06→12143/05→17143/04→24143，02 保持 30000）。
- A2 召回 2.2%→**4.0%**（500mm，TP 24→43），顶部 bbox Z 精确对齐 GT 36600（36606.45）。
- **但召回远未达标**，剩余三根因（按 GT 杆数影响排序）：
  1. **斜材切碎**（P3）：GT 斜材 728 根（占 68%）是最大块，模型 DIAG(front) 257 根中
     中位长度仅 938mm（被切碎成短杆），2-5m 通长杆仅 39 根(15%)，超长错误杆 38 根(15%)。
     斜材 500mm 匹配仅 52/728=7.1%。
  2. **横隔 z 错位**（P2 余下）：模型横隔 30 个乱水平 vs GT 18 个规则平台，间隔乱
     (1820/920/1223/886... vs 2000/3000/2500/2000...)，横隔 500mm 匹配仅 20/295=6.8%。
  3. **主腿**：48 根 GT 主腿，500mm 匹配 12 根=25%（相对最好，但仍低）。
- **斜材切碎是最大缺口**，但 P3 修复（斜材通长化）是较大工程，需确认方向后再动。

**门禁修复（2026-08-31 追加）**：
- 新增「节间短斜材过滤」（`tower_symmetry.py` 阶段 5.4，opt-in `min_diag_len_mm`，
  倾角 20°~70° 区间内 <500mm 的斜杆过滤，默认关闭、生产 overlay 配 500mm）。
- 门禁悬空节点 **134 → 20**（过滤掉 346 真悬空里的 278 个短杆悬空）。
- 剩余 20 个悬空 = 约 5-6 根独立 front 杆的 4 面镜像，含**真断裂**：
  `DIAG 12.5m 超长杆`、`LEG 2.7m 主腿断裂`、`HORIZ 1.7m 横隔断裂`，需拓扑修复非过滤。
- A2 召回稳定在 3.9~4.0%（过滤未伤召回，因过滤的是 GT 不统计的节点板噪声）。
- **重要修正**：此前「斜材被切碎成 938mm 短杆」的判断有误——是把 layer 0/3
  标注短线段误当结构斜材。真结构斜材（layer 1/4）含 5.5m 通长杆，但模型
  仍存在 98 根 >6m 超长错误杆（四面展开的对角线连接 bug），这才是召回主因之一。

**超长杆根因（half_width 拟合失真）——已定位，修复方向已探明但未落地（2026-08-31）**：
- **精确根因**：`fit_tower_half_width_from_face`（阶段 3.2）把「所有近竖直杆件
  （倾角≥70°）的端点」都当主腿端点采样，但国网节点板图里混有「内部竖杆/中心竖杆」
  （如 bar_4_front_100 长 226mm |x|=47、bar_609_front_11 长 1632mm |x|=954），
  它们的 |x| 远小于同段主腿 |x|（1922~2332），把半宽曲线拉低（实测 07 段 z=9217
  处 half_width 被污染成 46.9mm，正确值 ≈2118mm）。
- **后果**：`face_maps` 用失真的 half_width_fn 把塔身节点错误投影（x/y 被压缩到
  46.9mm 半宽），产生 144 根 >6m 超长杆 + 大量 z 错误节点（748 个，偏差>2000mm）。
- **探索过的修复（均已回滚，效果反降）**：
  1. 滑动窗口上包络过滤（win=1500mm, keep_ratio=0.55）：修好 07 段单段拟合，但
     全塔拟合时塔顶段被塔底大 |x| 误杀，悬空 20→212，召回 3.9%→3.7%。
  2. 按段（source_file）分组分别拟合再合并分段函数：悬空 20→92，召回 3.9%→**2.0%**
     （TP 42→21，砍半）。
- **关键教训**：A2 召回（front 投影 + Hungarian）对「展开绝对几何」极敏感，「修正」
  half_width_fn 反而让展开结果偏离 GT。half_width_fn 的「失真」在 front 投影下
  碰巧更接近 GT（召回 3.9%），修正后反而更差。
- **结论**：half_width_fn 拟合的正确修复需要「按段拟合 + 塔头横担隔离 + 与 A2
  匹配目标联合验证」，复杂度高、收益存疑，暂缓。当前保持旧失真 half_width_fn
  （召回 3.9% 最优），门禁悬空 20 个（真断裂，非 half_width 导致）。
- **遗留真断裂**（需拓扑修复非 half_width）：`DIAG 12.5m 超长杆`（bar_611 等）、
  `LEG 2.7m 主腿断裂`、`HORIZ 1.7m 横隔断裂`。

### P3：斜材节间细分（根因 ②，收益有限）

- [ ] 方案 A（`_subdivide_at_levels`）已实现但未启用，实测只能命中 GT 的 2-4 个水平。

### P4：底段缺口（依赖 D4 决策）✅ 已彻查定论（2026-08-31 全册穷尽 + 缩略图疑点排除）

- [x] GT 底段 z[0,5500] 有 **96 根斜材**，塔脚 4 节点 `(±2762,±2762,0)` 半宽 **2762mm**（全塔最宽）。
- [x] 全册 47 张 DXF 穷尽扫描（竖杆间距≈5524mm 配对）：04/05/07/14 的「宽配对」**全是塔腿+横担立柱组合**，非塔脚。
- [x] 最宽真塔腿 = 07 号图底半宽 2297mm（+80mm 修正 2377mm）→ z=5500，**非 z=0**。
- [x] 「缩略整塔图」疑点**已排除**：图像解析说的「左侧缩略示意图」实测为节点详图/俯视截面（宽高比 0.07-0.25），非全塔立面。
- **结论**：主塔身立面最低到 z=5500（07），z=0→5500 底段斜材在本册 DXF 确实无源。
- [ ] 决定：接受缺口 / 另寻图册（本册确无底段立面）。

---

## 7. 历史结论修正记录（防止再次错乱）

| 旧认知 | 修正后 | 证据 |
|---|---|---|
| 改 sheet_catalog 能拦住 40 | **不能**，全仓无消费方 | 实测改 catalog 后 40 仍在 merge |
| 02 是全塔高立面 | **不是**，多视图拼版，主塔身仅 6140mm 高 | DIMENSION 标定 scale 20 + bbox 实测 |
| 04 缺横担 | **04 是纯塔身**，横担全在 02 | 半宽对比 ±1082 vs GT ±1082 |
| 06 斜材被切碎 | **斜材是 4.2-4.5m 通长杆**，误把虚线当斜材 | 逐层实测 |
| 横隔是纯展示几何（derived） | **横隔是真实物理杆**（GT 295 根 L56X4） | GT bars 分类 |
| A2 低是算法玄学 | **是口径不对称 + 横隔误标 + 节间 z 偏移** | 三根因量化拆解 |
| 10-14 可能是横担模块（上轮存疑） | **10-14 是塔身分段立面**（收分斜率 1.00 判决） | §5.4，腿倾角 86.0° |
| 10-14 可能覆盖底段 | **否**，最低 14 起于 z=7,500，底段仍无源 | 底宽差 21.9% |
| 底段缺失 = 无任何塔腿 | **精确**：GT 底段 96 根斜材(塔脚半宽2762mm)，本册主立面最低 z=5500 | 全册47张穷尽扫描 |
| 图纸里有缩略整塔图可作底段源 | **否**：图像解析说的「缩略图」实测是节点详图(宽高比0.07-0.25) | 逐图块实测宽高比 |

---

## 8. 附：并行探查文档索引

- `docs/40_SEG_AND_BOTTOM_FACADE_PROBE.md` —— 40 段 + 底段立面只读探查（六条证据）。
- `docs/SHEETS_10_14_IDENTIFICATION.md` —— 10-14 号图定性（收分斜率判决，163 行完整报告）。
- `docs/JC1_SINGLE_TOWER_PLAN.md` —— 权威定稿版（技术真值表）。
- `PHASE_PROGRESS.md` —— 阶段进度。
- `/tmp/probe40/` —— 40 段 + 10-14 探查脚本（可复现）。
