# LevelGridSolver 设计稿——DXF 证据自推层网格（P2）

状态：设计冻结 v1（2026-09-05）。D1 完成（内核 + 验证 + 单测）；
**D2 完成（2026-09-05）：D2a 落地，D2b/D2c/环梁白名单经实测论证
不可去 GT 化，边界如下 §6a**。

## 1. 问题：层位 z 值的 GT 依赖面

当前管线里所有「分段/平台/横隔」类合成器的 z 层位来自三类**GT 来源**：

| 消费点 | 现值来源 | 位置 |
|---|---|---|
| `leg_chain_stitch` 断链层 | `gt_diagonal_terminal_levels`（45 z）+ `gt_terminal_levels_override`（33 z） | tower_symmetry.py:1397 |
| 跳层对重建层集 | 同上 | tower_symmetry.py:691 |
| 横隔层 | `gt_diaphragm_levels_override`（6 z） | tower_geometry.py:1218 |
| 平台/横担层 | `use_gt_platform_levels`（15 z） | tower_symmetry.py:554 |
| 横隔合成硬编码 | `22700/22800/…`（6 个 JC1 常数） | tower_geometry.py:1218（L1 债） |

后果：④「假设可追溯」门禁被 gt_injected 披露面拖住；L1 硬编码层常数债；
换塔（ZC1）要重做全套 override 表。

**目标**：层位由 DXF 证据自推（投票），逐点替换 GT 表。与已撤回的
P2.6 span 注入的根本区别：**无塔专属常数、无 GT 反推**——每层带证据
出处（哪册哪类杆端点/哪条标注），GT 只做离线验证。

## 2. 证据源（实测盘点，JC1）

### 2.1 锚层（精确，无提取噪声）
- **A 标注层** `beam_marker_levels_mm`：图纸梁标注文本（marker 识别产物，
  recognized 证据），每册 2-12 个，JC1 合计 25 层。命中 GT platform 15/15。
- **B 册边界层** `view_regions z_offset`：图纸分册布局事实（07→7000、
  06→12000、05→16000/18000、04→24000、02→30000）。

### 2.2 几何投票端点（±100-300mm 提取噪声）
各册 front 视图提取杆件的**端点 z 直方图**（100mm 桶），按 origin 分权：

| origin | 权重 | 理由 |
|---|---|---|
| `dxf_geom` | 2 | 直读画线端点（腿线断点/斜材转折=分段边界） |
| `diag_synth` | 1 | 画线拓扑补全（纯口径已承认的图形证据） |
| `diag_complete` | 1 | 同上 |
| `leg_synth` | **排除** | 表驱动 span 产物——入投票 = GT 洗白通道 |
| `marker_synth` 杆 | **排除** | 文本证据已由锚层 A 表达，避免双计 |

### 2.3 不采纳的证据
- **尺寸标注文本**：sheet `dimensions` 实测为杆件规格（75/100/95mm），
  无标高语义，不投票。
- **GT 任何字段**：仅离线验证用（§5）。

## 3. 算法（原型实测冻结）

```
vote_level_grid(per_sheet_bars, marker_levels, z_offsets):
  # 1) 每册：端点直方图（100mm 桶 × origin 权）
  #    核桶 = ≥2 独立杆的桶（单杆桶只加权、不连链——防稀疏桥并簇）
  # 2) 核桶链：相邻核桶 gap ≤ 400mm 连链
  # 3) 链内贪心剥峰：最强核发射为层 z，±400 吸收其余核，循环
  #    （比谷分割稳：9900..11100 链剥出 9900+10700 双层）
  # 4) 锚骨架：marker(w3) + 册边界(w2)，±300 去重合并（marker 优先取值）
  # 5) 几何补位：几何层按权重降序，离骨架任一层 ≥400mm 才加入
  # 6) 输出：sorted levels + 每层 provenance（册/origin/杆数/权重）
```

关键实测教训（写死在设计里）：
- **桶均值/链式中位会把层位拉偏 100-700mm**（v2-v4 教训）：簇心必须取
  众数桶（最强核），不是均值。
- **链式 gap≤250 全糊**（7000-11400 连成 89 杆巨簇）：核桶门槛 + 400 链距。
- **±400 吸收半径**：小于最小真实层间距（14400/14500=100 允许并，
  8000/8500=500 不许吞）。

## 4. JC1 实测（原型，2026-09-05）

| 指标 | 值 |
|---|---|
| 网格规模 | 48 层（锚 26 + 几何 22） |
| vs GT terminal 45 层（tol 300） | 40/45 = 88.9% |
| vs GT 可绘制区 40 层（≥7000，tol 300） | **39/40 = 97.5%** |
| vs GT platform 15 层 | 15/15 = 100% |
| 不可达 | 3000/4000/5000/6000（<7000 不绘制区，结构性天花板） |
| 遗留缺口 | 21500（证据 21700/21800 被 22000 峰吸收——子层歧义） |
| 网格独有层（无 GT 对应 ±300） | 12 层：7000/7500/12500/16400/17600/18500/23300/25000/25600/27000/28600/29500（收腰点/交叉点/图案完成点） |

路线图「JC1 92%」达标（97.6% > 92%）。**ZC1 实测（D1，正式内核）：
terminal 33/33 = 100%（tol 300；tol 150 即 97%）、platform 6/6 = 100%**
——远超路线图 71% 预期（ZC1 有 58 个标注层 + 密集斜材端点，证据质量好）。

D1 正式内核两塔门禁（85%）均过；网格独有层 ZC1 14 个（6800/7600/8400/
9800/10500/…收腰/交叉点），JC1 12 个，处置见 §4。

### 独有层的处置
不删除（它们是真实结构点，收腰/交叉处腿链确实断）：分级置信度
`marker > boundary > geom_w≥20 > geom_w<20`。集成时：
- **break_levels（腿链断链）**：全网格直接用（多切不亏——豁免表 P2.5
  已防误合并）；
- **合成杆 z 定位（横隔/平台）**：只用 marker+boundary+高置信几何层
  （假阳层顶多造成个别 FP，不破坏已有 TP）。

## 5. 验证方案（离线，GT 只进这里）

`scripts/validate_level_grid.py --tower jc1|zc1`：
1. 跑投票网格（真实 sheets/ 中间产物或重跑提取）；
2. 对 GT 层集合（terminal ∪ platform）算命中率（tol 150/250/300 三档）；
3. 输出分层报告：命中/未命中/独有，每层 provenance；
4. 退出码：可绘制区命中 < 85% → 1（门禁）。
报告落 `out/<tower>/level_grid_validation.json`，进交付审计链。

## 6. 集成方案（分阶段，每步全量 A/B + 红线）

| 阶段 | 替换点 | 验收 | 结果 |
|---|---|---|---|
| D2a | `leg_chain_stitch_break_terminal` 断链层 → 投票网格（`leg_chain_stitch_break_source="level_grid"`，overlay 开关） | JC1 dual-pure 不回退，dual-recon ≥99% 红线，ZC1 不回退 | **✅ 落地（2026-09-05）**：JC1 TP 304=304 / ZC1 TP 9=9，recon 红线均保持（JC1 99.6% / ZC1 75.8%）；网格 56/48 层实际驱动断链（split_at_degree 99→56）；A/B 入口 `run_*.py --break-source level_grid`（脚本层覆盖 + 独立目录） |
| D2b | 横隔层表 `gt_diaphragm_levels_override` → 网格层 | 横隔 TP 不降 | **❌ 不可去 GT 化**（§6a 论证 1） |
| D2c | `panel_cross_skip_pairs` 跳层对层集 | 同 D2a | **⏸ 两塔 skip_pairs=False（死路径），且跨度观测覆盖 12/42（§6a 论证 3）——启动 skip_pairs 时再做** |
| 环梁白名单 | `generate_diaphragms` 22700/…/34200 常数（L1 债） | 横隔 TP 不降 | **❌ 不可去 GT 化**（§6a 论证 2）；实测有效集仅 {22800,32700,33500,34200}（22700/22900 是死代码——panel_levels 不含此二层），重新归类为「构型选择常数（4 层 16 杆，reconstructed 口径）」 |
| D3 | `use_gt_platform_levels` → 网格 marker 锚层（`panel_level_source="level_grid"`） | JC1 双口径红线 | **❌ 不可去 GT 化（2026-09-05 实测，§6a 论证 4）**：dual-recon R 99.6%→93.6%（−65 TP，红线击穿）；dual-pure 304→303（−1，纯直读对平台层几乎无依赖） |

## 6a. 不可去 GT 化边界（实测论证，2026-09-05）

1. **横隔层子集（gt_diaphragm_levels 14 层）**：图纸画了全部平台层
   （marker 24 层 ⊇ GT 横隔 14 层，15/15 命中），GT 只在 14 层有横隔杆
   ——「哪层没有横隔」是设计详图的省略选择，不是图纸证据。用 marker
   全集当横隔层 → 10 个假横隔层 × 25 杆 ≈ 250 FP；用子集需要 GT 独有
   信息。维持 override 表。
2. **全宽环梁层白名单**：有效集 {22800, 32700, 33500, 34200}，每层 4 杆。
   逐一测过的图纸判据全部失败：marker_synth 梁杆存在性（每层都有）、
   梁杆 xy 跨度（塔头收窄段不可比）、大跨 dxf 斜杆端点（22800 无、
   6500/8500/14000/16000 假阳）、水平长杆（22800 的全是横隔自产循环
   依赖）。这 4 层的选择是构型语义（横担支撑环 vs 标准横隔），当前
   提取层没有该语义。
3. **跨度白名单（terminal_pair_span_whitelist 42 对）**：合并模型
   dxf_geom/diag_synth 长斜杆跨度观测（span≥1000，snap 网格层 ±400）
   覆盖 GT 白名单 12/42——漏的 30 对正是需要合成器补的节间（图纸直读
   覆盖不足是生成器存在的原因）。观测可做 FP 过滤/评分（现有机制），
   不可枚举白名单。
4. **平台层表（gt_platform_levels 15 层，D3 实测 2026-09-05）**：
   marker 锚层 24 层 ⊇ GT 平台表（15/15 全命中），但 9 个非平台 marker
   层灌入横隔与腿分段 → 横隔 23 vs 14 层（+9 假层 ≈ +204 FP）+
   terminal_pair 生成关闭（论证 3）→ dual-recon 1067→1002
   （R 99.6%→93.6%，红线击穿）。归因副产品：dual-pure 304→303
   （FP −3）——纯直读链对平台层注入几乎无依赖，GT 平台表的价值
   全部在重建口径。开关 `panel_level_source="level_grid"` 保留
   （ZC1 换塔用：ZC1 平台/横隔 z 现靠 JC1 经验表，网格可替换
   「经验表来源」而不破坏 ZC1 红线——见 A2-ZC1 提升计划）。

**不改变的**：
- `metrics.py` 容差/匹配器（提分纪律）；
- 纯口径 origin 准入名单——层位来源去 GT 化不改变杆件 origin 语义
  （dia_synth 仍是 dia_synth，只是它的 z 从投票网格来）；
- `version.json` 的 `gt_injected` 披露（替换完成后逐面清零）。

## 7. 纪律边界（对照 P2.6 教训）

1. **无塔专属常数**：算法参数（桶宽 100、核门槛 2 杆、链距 400、吸收
   400、补位距离 400）是提取噪声尺度决定的一般参数，两塔共用，不按塔调。
2. **GT 只做验证**：投票内核不 import gt_profile/不读 GT 文件；验证脚本
   单独文件、单独调用。
3. **防洗白**：leg_synth（表驱动）端点不入投票——它就是被撤回的 P2.6
   的通道。
4. **每层可追溯**：网格层带 provenance 记录（哪册/哪类/几杆），交付可审计。
5. **A/B 全量**：每个集成阶段跑前后完整对比 + pytest + L1-L6 + ZC1 回归。
