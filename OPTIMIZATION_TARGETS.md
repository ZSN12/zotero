# 35A1-JC1 召回率优化目标与迭代记录

设定时间：2026-08-31 01:53（用户入睡后自主迭代）
设定依据：JC1 全量复跑（50 张 DXF、GT 1071 根）+ 碎片化/口径归因诊断

---

## 一、起点基线（commit `93fd16e`，ezdxf 模式）

| 口径 | 模型杆数 | TP@500 | Precision | Recall |
|---|---|---|---|---|
| **A2-pure（纯 DXF 直接识别，主口径）** | 280 | **56** | 20.0% | **5.2%** |
| A2-full（physical，含 GT 标高辅助） | 632 | 188 | 29.8% | 17.5% |
| A2-effective（z≥6500） | 632 | 188 | 29.8% | 19.1% |

**口径诚实化结论**：A2-full 的 188 根中约 **132 根（70%）来自 `diaphragm@gt_levels`**
（用 GT canonical 标高重建的 330 根横隔），属借助 GT 的增强成分。对外一律以
**A2-pure 为主口径**，A2-full 仅作内部归因。

## 二、根因（已定量验证，非推测）

1. **碎片化 = 头号瓶颈**：GT 杆件长度中位 2005mm（leg 2506 / diagonal 3286），
   模型纯 DXF 中位 888mm，**长度比 2.26×**。一根 GT 杆被切 2~3 段后端点误差
   天然达 1000mm 量级，500mm 容差调不回来。→ 任务 2「吸附 20~80mm 缝隙」治标。
2. **front 2D 口径硬上限 80.1%**（858/1071）：
   - `y_member` 87 根：沿 y 轴，front 投影长度 = 0（退化为点），几何不可匹配；
   - `depth_diag` 252 根：与 `leg` front 投影距离**中位 0mm、100% 重合**，
     Hungarian 1:1 下 504 根最多召一半（−126）。
   → 突破靠转 3D 评测，不靠调优 2D 算法。
3. **Precision 低（29.8%）**：FP 444 根中 198 根是 gt_levels 横隔
   （模型造 330 根 vs GT 208 根），属过量生成。

## 三、本轮目标（2026-08-31 夜间自主迭代）

| 优先级 | 抓手 | 指标 | 起点 | 目标 | 判定 |
|---|---|---|---|---|---|
| **P1** | S4 共线拼接（265 对碎片候选） | A2-pure @500 | 5.2% | **≥ 10%** | 主目标 |
| **P1** | 同上 | A2-full @500 | 17.5% | **≥ 25%** | 主目标 |
| **P2** | 转 3D 评测（解开 depth_diag/y_member 死结） | 3D 口径基线 | 未测 | **建立基线** | 探索 |
| **P3** | 横隔数量校准（330 → ≈208） | Precision @500 | 29.8% | **≥ 40%** | 次目标 |

**停止条件**：P1 达成且 A2-full ≥ 25%；或连续 2 轮无增益（±2 根内）则记录结论停机。

## 四、纪律（自主迭代期间必须遵守）

1. **每轮必须跑真实 JC1 全量**（50 张 DXF / GT 1071），禁止用子集或合成数据自证。
2. **每轮结果追加到本文件第六节**，含 commit sha、口径、TP/P/R、结论。
3. **不改他人正在修改的文件**——`tower_geometry.py` / `tower_symmetry.py` 正被
   Phase 2.3 受控吸附工作并行修改；本轮一律走**离线后处理**（scripts/experiment_*.py），
   不叠加改动，验证出收益后再协商集成。
4. **不靠调容差刷绿**——所有增益必须来自几何/拓扑改进，容差恒定为 DEFAULT_TOLS。
5. **pytest 全量回归通过**才允许提交。
6. **A1/A3 件号指标**：当前 model.json 为 ezdxf 模式（无 MLLM），件号恒为 0，
   本轮不作为优化对象，仅记录。

## 五、已知不可达（不要浪费轮次攻）

- `y_member` 87 根：front 2D 下物理不可召回（投影退化为点）。
- `depth_diag` 252 根：front 2D 下与 leg 重合，1:1 最多召一半。
- 底段 z < 6500mm：无立面 DXF 输入，GT 84 根无源（已用 A2-effective 隔离）。

## 六、迭代记录

### Round 0 — 基线（commit `93fd16e`）
- A2-pure @500 = 56（R 5.2%，P 20.0%）；A2-full @500 = 188（R 17.5%，P 29.8%）
- 口径上限 80.1%（858/1071）；FN 883 构成：diagonal 253 / depth_diag 248 /
  leg 231 / y_member 87 / horiz_x 64
- 结论：碎片化 2.26× 为主因，先攻 P1 拼接。

### Round 1 — 拼接首试失败：并查集过合并
- 实现 `scripts/experiment_collinear_stitch.py`（离线后处理，不碰生产代码）。
- 并查集全连通合并：164 组、删 828 建 164，杆件 1626→962。
- **结果 A2-pure 56→26、A2-full 188→155（均下降）**，仅 Precision 29.8%→32.8%。
- 根因：段数分布 `{2:68, 3:32, 4:28, …, 17:4, 51:4}` —— 4 组把整条主腿的
  **51 个节间并成一根 17256mm 超长杆**（GT 最长仅 7077mm）。
- 修正：改为**贪心成对合并** + `max_merged_len` / `max_segments` 双约束。

### Round 2 — 根因修正：不是端点不准，是覆盖不足
诊断已匹配的 188 对（`scripts/` 外临时脚本，结论已固化到本文件）：
| 指标 | 实测值 |
|---|---|
| 长度比（大/小）中位 | **1.06** |
| 端点 Z 偏差中位 | **0mm** |
| 端点 X 偏差中位 | 25mm |
- 能匹配上的杆**端点位置是准的**；未匹配 GT 中 56% 的最近模型杆误差 >1000mm，
  即**压根没有对应杆**，而非碎片。
- 按 3m 分段：模型 DXF 杆在每个 z 段都远少于 GT（如 z 12-15m：GT 118 vs 模型 31）。
- **推论**：任何「移动端点」的操作（吸附 / 拼接）都会破坏已有的精确定位，
  这解释了任务 2、任务 3 在严格参数下均无正收益。

### Round 3 — 口径不对称 4 倍（重大）
- `bars_from_model_2d(view="front")` 经 `_face_to_view` 只放行 `face=f`
  （b/l/r → side 被过滤），模型等于只投 **1 个面**；而 GT front 投影 1071 根是
  **全塔 4 个面**投影到 (x,z)。两侧不对称 4 倍。
- 4 面全投影（对称口径）：physical TP@500 **188 → 281**（R 17.6% → 26.2%）。
- 该修正**尚未改生产口径**（会改变全部历史指标），仅以实验脚本呈现，待决策。
- 实验固化：`scripts/experiment_caliber_matrix.py`。

### Round 4 — 拼接参数网格搜索：突破（核心成果）
`scripts/experiment_stitch_sweep.py`，以 recognition（真实识别）口径寻优：
- 第一轮：宽松方向单调增益，最优落在网格边界 gap=400/ang=15° → TP 56→167。
- 第二轮外扩后**收敛**：`gap=800mm / ang=20° / maxLen=10000mm / seg≤10`。
- gap≥1000 后回落（1000→146、1200→138），确认为真实极值。

**成果（真实 JC1 数据，容差恒定 DEFAULT_TOLS 未动）**

| 口径 | 模式 | 不拼接 | 贪心拼接（最优参数） | 变化 |
|---|---|---|---|---|
| 单面（现行生产口径） | physical | TP 188, P 29.7% | **TP 197, P 33.8%** | TP **+9**，P **+4.1** 点 |
| 单面（现行生产口径） | recognition | TP 56, P 20.0% | **TP 66, P 26.1%** | TP **+10**，P **+6.1** 点 |
| 4 面（对称口径） | physical | TP 281, R 26.2% | **TP 305, R 28.5%** | TP **+24** |
| 4 面（对称口径） | recognition | TP 56, R 5.2% | **TP 170, R 15.9%** | TP **+114**（3 倍） |

- **不依赖口径变更即有增益**：现行单面口径下 TP +9 / P +4.1 点，可立即集成。
- 关键参数洞察：有效 gap 是 **800mm 而非原计划的 80mm**——DXF 分段绘制的断裂
  间隙远大于预期，原「20~80mm 缝隙」假设不成立。
- 修正 bug：合成杆 `geometry_class` 曾硬编码 `recognized`，会把镜像面杆件洗白；
  已改为按源杆语义继承。

### Round 5 — P2 转 3D 评测：结论为否
- 3D 基线（`evaluate_ground_truth_3d.py`）：TP@500 = 242（R 22.6%, P 15.7%）、
  TP@800 = 296（R 27.6%）。
- **低于 front 2D 四面口径的 281/305**：模型 4 个面由 `expand_4_face_symmetry`
  镜像生成，3D 坐标精度不足（各面 302 根仅匹配 32 根，精度 10.6%），
  而 2D 投影恰好忽略 y 向误差。
- **结论：3D 评测不是当前阶段的解法**，先补 3D 配准精度再谈切换。

---

## 七、成果汇总与待决策事项

**已达成的实质增益（现行口径，可立即集成）**
- 拼接后处理：physical TP 188→197（+9），Precision 29.7%→33.8%（+4.1 点）。
- 若同时采用 4 面对称口径：recognition 5.2%→15.9%（TP +114）。

**待决策（需人确认，未擅自改动）**
1. **是否切换 4 面对称口径**——对称性论证成立（GT 含 depth_diag 252 根，
   必为完整 3D 模型），但会重算全部历史指标，影响对外汇报数字连续性。
2. **是否将拼接集成进生产管线**——当前为离线后处理（`scripts/experiment_*.py`）。
   集成需改 `tower_symmetry.py`，**该文件正被 Phase 2.3 受控吸附工作并行修改**，
   未叠加改动以避免冲突。
3. **P3 横隔校准**（模型 330 根 vs GT 208 根）未启动——这是 Precision 的下一个抓手。
